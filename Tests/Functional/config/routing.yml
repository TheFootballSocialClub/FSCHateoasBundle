api_user_get:
    pattern: /api/users/{identifier}

api_user_list:
    pattern: /api/users

api_post_get:
    pattern: /api/posts/{id}

api_user_posts_list:
    pattern: /api/users/{id}/posts

user_profile:
    pattern: /profile/{id}
